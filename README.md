# 🌸 Цветочная Лавка - Инструкция по запуску

## 🚀 Как запустить игру локально

Приложение работает на HTML/JS модулях, поэтому просто открыть `index.html` двойным кликом **недостаточно** (браузер заблокирует загрузку модулей из-за CORS). Нужен локальный веб-сервер.

### Способ 1: Использование Python (Рекомендуемый)
Если у вас установлен Python, это самый быстрый способ.

1.  Откройте терминал в папке проекта (`c:\Users\user\Documents\torgsvet`).
2.  Запустите команду:
    ```bash
    python -m http.server 8080
    ```
3.  Откройте в браузере: [http://localhost:8080](http://localhost:8080)

### Способ 2: Использование VS Code (Live Server)
Если у вас установлено расширение **Live Server**:

1.  Нажмите правой кнопкой мыши на файл `index.html` в списке файлов.
2.  Выберите пункт **"Open with Live Server"**.
3.  Игра откроется автоматически.

### Способ 3: VS Code Debugger
Вы создали файл `launch.json`. Чтобы он работал:

1.  Сначала запустите сервер (например, через Python на порту 8080, как в Способе 1).
2.  Перейдите во вкладку "Run and Debug" (Ctrl+Shift+D).
3.  Выберите **"Launch Chrome against localhost"**.
4.  Нажмите **F5**.

> **Обратите внимание**: В вашем `launch.json` указан порт `8080`. Если вы запустите сервер на другом порту (например, 8000 или 5500), нужно будет поменять порт и в `launch.json`.
